# Baked into the v2 agent image at build time. Same file, same mechanism as
# .env for v1 -- only AGENT_IMAGE differs, because that's the only thing
# that actually changed on this version bump.
BLUEPRINT_ID=bp-8891
AGENT_IMAGE=ubs-poc/demo-agent:v2
UBS_API_BASE=http://mock-ubs-api.ubs-poc-v2.svc.cluster.local:5000
